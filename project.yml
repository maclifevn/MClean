name: MClean
options:
  bundleIdPrefix: com.maclife
  deploymentTarget:
    macOS: "13.0"
  xcodeVersion: "16.4"
  createIntermediateGroups: true
  defaultConfig: Release

settings:
  base:
    SWIFT_VERSION: "5.9"
    MACOSX_DEPLOYMENT_TARGET: "13.0"
    ARCHS: "arm64 x86_64"
    # Universal only where it matters (Release, overridden below). Debug builds
    # just the active arch for ~2x faster local iteration.
    ONLY_ACTIVE_ARCH: "YES"
    DEAD_CODE_STRIPPING: "YES"
    CODE_SIGN_IDENTITY: "Apple Development"
    CODE_SIGN_STYLE: "Automatic"
    DEVELOPMENT_TEAM: "CF5PGH3KGK"
    CODE_SIGN_ENTITLEMENTS: "MClean/MClean.entitlements"
    INFOPLIST_FILE: "MClean/Info.plist"
    PRODUCT_BUNDLE_IDENTIFIER: "com.maclife.mclean"
    MARKETING_VERSION: "1.0.1"
    CURRENT_PROJECT_VERSION: "2"
    GENERATE_INFOPLIST_FILE: "NO"
    ASSETCATALOG_COMPILER_APPICON_NAME: "AppIcon"
    COMBINE_HIDPI_IMAGES: "YES"
  configs:
    Release:
      # Shipped builds stay universal; strip symbols from the executable
      # (crash symbolication still works via the separate dSYM).
      ONLY_ACTIVE_ARCH: "NO"
      STRIP_INSTALLED_PRODUCT: "YES"
      STRIP_STYLE: "non-global"

targets:
  MClean:
    type: application
    platform: macOS
    sources:
      - MClean
    settings:
      base:
        PRODUCT_NAME: MClean
        SWIFT_EMIT_LOC_STRINGS: "YES"
        LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/../Frameworks"
      # Debug signing intentionally NOT disabled: a real Apple Development
      # signature keeps the TCC/FDA grant stable across rebuilds (ad-hoc
      # signatures change cdhash every build and macOS drops the grant).

  MCleanTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - MCleanTests
    dependencies:
      - target: MClean

schemes:
  MClean:
    build:
      targets:
        MClean: all
        MCleanTests: [test]
    test:
      targets:
        - name: MCleanTests
